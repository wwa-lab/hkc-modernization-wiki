# Data Dictionary Standard

Dictionary files are machine-readable JSON files.

Use arrays as the top-level structure.

Each future dictionary entry should include stable identifiers, legacy names, source evidence references, review status, and comments where applicable.

This baseline keeps dictionary files as valid empty arrays until reviewed content is ready to be added.

